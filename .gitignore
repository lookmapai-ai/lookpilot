files.zip
.DS_Store
*.log
.claude/

# Exports de design do Claude design (não pertencem ao repo da extensão)
*.zip
LookMap.html
LookPilot*.html

# landing/lookpilot-extensao.zip É versionado: é o arquivo que o botão
# "Baixar extensão" entrega em produção (não tem servidor gerando na hora).
!landing/lookpilot-extensao.zip
